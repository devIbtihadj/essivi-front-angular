import { VehiculeFilterPipe } from './vehicule-filter.pipe';

describe('VehiculeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new VehiculeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
