import { TypeVehiculeFilterPipe } from './type-vehicule-filter.pipe';

describe('TypeVehiculeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TypeVehiculeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
