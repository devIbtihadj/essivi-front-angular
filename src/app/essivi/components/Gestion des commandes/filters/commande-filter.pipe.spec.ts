import { CommandeFilterPipe } from './commande-filter.pipe';

describe('CommandeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CommandeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
