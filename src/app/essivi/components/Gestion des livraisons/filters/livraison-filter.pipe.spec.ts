import { LivraisonFilterPipe } from './livraison-filter.pipe';

describe('LivraisonFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new LivraisonFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
