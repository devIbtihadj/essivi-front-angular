import { CommercialFilterPipe } from './commercial-filter.pipe';

describe('CommercialFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CommercialFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
