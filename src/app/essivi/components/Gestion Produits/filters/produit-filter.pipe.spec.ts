import { ProduitFilterPipe } from './produit-filter.pipe';

describe('ProduitFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ProduitFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
