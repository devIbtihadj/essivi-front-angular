import { MarqueFilterPipe } from './marque-filter.pipe';

describe('MarqueFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MarqueFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
